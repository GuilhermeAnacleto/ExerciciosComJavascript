const nome = "                         GUILHERME anacleto"

// Esse to lower case serve para FILTRAR letras minusculas caso o usuario digite de forma diferente
console.log(nome.trim().toLocaleLowerCase());
//trim serve para tirar os espaços em branco deixado pelo usuario